<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.example.basedemo">

    <uses-permission android:name="android.permission.WRITE_CONTACTS" />
    <uses-permission android:name="android.permission.READ_CONTACTS" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme"
        tools:ignore="GoogleAppIndexingWarning">
        <activity android:name=".my_lifecycle.LifecycleDemoActivity"></activity>
        <activity android:name=".recents.DocumentCentricActivity" />
        <activity android:name=".recents.NewDocumentActivity" />
        <activity android:name=".intent.IntentDemoActivity" />
        <activity android:name=".components4.fragment.FragmentDemoActivity" />
        <activity android:name=".animation.ScenceActivity" />
        <activity android:name=".animation.MyZoomActivity" />
        <activity android:name=".animation.RotationActivity" />
        <activity android:name=".animation.AttributeAnimationActivity" />
        <activity android:name=".components4.activity.ActActivity" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>